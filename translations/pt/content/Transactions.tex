\chapter{Transacções Confidenciais em Anel (RingCT)}
\label{chapter:transactions}

Depois dos capítulos \ref{chapter:addresses} e
\ref{chapter:pedersen-commitments}, uma transacção de um remetente anónimo
para um destinatário anónimo soa mais ou menos assim:

``A minha transacção publica uma chave de transacção e cria duas saídas,
cada uma com o seu endereço oculto, a sua etiqueta de visualização, o seu
montante em código e o seu compromisso. Uma única prova Bulletproofs+
mostra que ambos os montantes cabem em 64 bits. Para pagar, gasto uma saída
que escondo entre quinze desvios. A assinatura CLSAG mostra que conheço a
chave de gasto e a abertura correcta de um dos compromissos do anel, sem
dizer qual. A imagem de chave mostra que não gastei antes essa mesma saída.
Finalmente, os pseudo-compromissos das entradas equilibram os compromissos
das saídas e a taxa.''

É uma frase curta para um mecanismo com muitas rodas dentadas. Ainda ficam
as perguntas certas. O remetente possui mesmo uma das saídas de cada anel?
O montante escondido nessa saída é o montante colocado no respectivo
pseudo-compromisso? Nenhuma saída cria dinheiro? E será que alguém mudou o
destinatário, a taxa, os anéis ou as provas depois de a transacção ter sido
assinada?

As respostas vêm de três construções que já conhecemos:

\begin{itemize}
    \item os endereços ocultos e as imagens de chave do capítulo
    \ref{chapter:addresses};
    \item os compromissos, pseudo-compromissos e provas Bulletproofs+ do
    capítulo \ref{chapter:pedersen-commitments};
    \item a assinatura CLSAG da secção \ref{sec:CLSAG}.
\end{itemize}

Este capítulo encaixa essas peças nos bytes que a versão 16 aceita. As
afirmações acerca da implementação referem-se à revisão congelada
\texttt{3646f648db57f60cca86430e25a635d19fa9b92a} do código Monero. Nessa
revisão, a versão 16 activou-se na rede principal à altura \(2\,689\,608\) e
é a última versão agendada. Isto é um retrato verificável dessa revisão, não
uma promessa de que o protocolo nunca mais mudará.\footnote{Veja-se
\texttt{src/hardforks/hardforks.cpp} para a agenda da rede principal e
\texttt{src/cryptonote\_config.h} para as constantes de cada versão.}


\section{O caminho activo: \texttt{RCTTypeBulletproofPlus}}
\label{sec:RCTTypeBulletproofPlus}

Uma transacção ordinária criada pelo programa de referência na versão 16
tem versão de serialização 2 e tipo RingCT `6', isto é,
\texttt{RCTTypeBulletproofPlus}. Usa uma CLSAG por entrada, anéis de 16
membros e uma prova Bulletproofs+ agregada para todas as saídas. Tem entre
duas e dezasseis saídas. A transacção de mineiro é a excepção importante:
tem tipo `0' (\texttt{RCTTypeNull}), montantes visíveis, nenhuma CLSAG e
nenhuma prova de domínio. A rotina que constrói o bloco cria actualmente uma
saída de mineiro; o consenso não contém, porém, uma regra isolada de
``exactamente uma saída'': verifica a entrada de geração, o tipo, o
desbloqueio, os alvos e a soma da recompensa.\footnote{A admissão dos tipos por versão está em
\texttt{src/cryptonote\_core/blockchain.cpp}; a enumeração dos tipos, em
\texttt{src/ringct/rctTypes.h}; e a escolha feita pela carteira, em
\texttt{src/wallet/wallet2.cpp} e \texttt{src/ringct/rctSigs.cpp}.}

O nome merece ser lido com cuidado. \emph{Confidencial} quer dizer que os
montantes ficam escondidos em compromissos. \emph{Em anel} quer dizer que a
autorização aponta para um conjunto de saídas possíveis, não para uma saída
declarada. Nenhuma destas duas propriedades esconde tudo: a taxa, o número
de entradas, o número de saídas, os membros de cada anel, as imagens de
chave e o campo \texttt{extra} continuam públicos.

\subsection*{Uma placa de arquivo: \texttt{RCTTypeBulletproof2}}
\label{sec:RCTTypeBulletproof2}

O tipo `4', \texttt{RCTTypeBulletproof2}, pertence à história do
protocolo. Usava Bulletproofs e MLSAG, e era o tipo activo na versão 12
descrita pela edição anterior deste relatório. O apêndice
\ref{appendix:RCTTypeBulletproof2} conserva uma transacção real desse período
como espécime histórico: é óptima para aprender a ler os bytes, mas não é um
molde de uma nova transacção v16. Entre os dois extremos existiu ainda o
tipo `5', \texttt{RCTTypeCLSAG}, que já trocava MLSAG por CLSAG mas ainda não
usava Bulletproofs+. Os tipos `1'--`5' continuam necessários para validar a
história da cadeia; na versão 16 não são o caminho para novas transacções
ordinárias.\footnote{As transições estão codificadas em
\texttt{src/cryptonote\_core/blockchain.cpp}: CLSAG é admitida desde v13,
Bulletproofs+ desde v15, e v16 deixa de admitir os Bulletproofs anteriores
em novas transacções.}


\subsection{Compromissos a montantes e taxas de transacção}
\label{sec:commitments-and-fees}

Um moneriano recebeu \(m\) saídas com montantes
\(a_1,\ldots,a_m\), endereços ocultos
\(P_{\pi_1,1},\ldots,P_{\pi_m,m}\) e compromissos
\begin{align*}
 C^a_{\pi_j,j}=x_jG+a_jH,\qquad j\in\{1,\ldots,m\}.
\end{align*}
Ele conhece as chaves privadas de gasto \(p_j\) tais que
\(P_{\pi_j,j}=p_jG\), os montantes \(a_j\) e as máscaras \(x_j\).

Pretende criar \(p\) saídas com montantes \(b_1,\ldots,b_p\) e pagar uma
taxa \(f\), todos em unidades atómicas, de modo que
\begin{align}
 \sum_{j=1}^{m}a_j
   =\sum_{t=1}^{p}b_t+f.
 \label{eq:transaction-amount-balance}
\end{align}
A taxa \(f\) é pública e serializada como um inteiro de comprimento
variável. O mínimo de taxa que os nós costumam exigir para propagar uma
transacção é política da carteira e da \emph{mempool}, não esta equação de
consenso: um bloco pode conter uma transacção de taxa inferior, desde que
esta seja válida nos restantes aspectos. O valor declarado continua, claro,
a entrar no equilíbrio e na recompensa do mineiro. Veja-se a secção
\ref{subsec:dynamic-minimum-fee}.

Para cada saída nova a carteira deriva, como na secção
\ref{sec:pedersen_monero}, uma máscara determinística
\begin{align*}
 y_t=\mathcal H_n(``\texttt{commitment\_mask}''\,\|\,h_t)
\end{align*}
e publica o compromisso
\begin{align*}
 C^b_t=y_tG+b_tH.
\end{align*}

Para cada entrada cria também um pseudo-compromisso
\begin{align*}
 \widetilde C^a_j=x'_jG+a_jH.
\end{align*}
As primeiras \(m-1\) pseudo-máscaras \(x'_j\) são escolhidas ao acaso. A
última fecha a cortina sem deixar uma frincha:
\begin{align*}
 x'_m=\sum_{t=1}^{p}y_t-\sum_{j=1}^{m-1}x'_j\pmod l.
\end{align*}
Por homomorfismo dos compromissos, a equação
\begin{align}
 \sum_{j=1}^{m}\widetilde C^a_j
 \stackrel{?}{=}
 \sum_{t=1}^{p}C^b_t+fH
 \label{eq:transaction-commitment-balance}
\end{align}
verifica ao mesmo tempo o equilíbrio dos montantes e o das máscaras, sem
revelar nenhum \(a_j\) ou \(b_t\). A própria igualdade não prova que o autor
possui as entradas. É a CLSAG que prende cada pseudo-compromisso à saída
real de um anel.

O programa de referência baralha as saídas antes de lhes atribuir os índices
\(t\) e ordena as entradas por imagem de chave. São escolhas de construção
da carteira; o consenso recebe apenas a ordem final.


\subsection{O que nasce em cada saída}

Para a saída \(t\), o remetente escolhe a chave de transacção apropriada e
calcula a derivação partilhada \(D_t\), o escalar
\begin{align*}
 h_t=\mathcal H_n(D_t\,\|\,\operatorname{varint}(t))
\end{align*}
e o endereço oculto do destinatário
\begin{align*}
 P^b_t=h_tG+K^s_t.
\end{align*}
Na versão 16 o alvo serializado é etiquetado: contém \(P^b_t\) e o primeiro
byte de
\begin{align*}
 \mathcal H(``\texttt{view\_tag}''\,\|\,D_t\,\|\,
                    \operatorname{varint}(t)).
\end{align*}
A etiqueta deixa a carteira rejeitar quase todas as saídas alheias antes de
fazer o trabalho mais caro; não é uma prova de propriedade.

O montante em código contém apenas oito bytes:
\begin{align*}
 b_t\oplus_8
 \mathcal H(``\texttt{amount}''\,\|\,h_t)[0\ldots7].
\end{align*}
A máscara não viaja cifrada ao lado do montante. O destinatário volta a
calcular \(y_t\) pela fórmula anterior e confirma que o compromisso publicado
é \(C^b_t=y_tG+b_tH\). Esta confirmação é importante:
decifrar oito bytes com aspecto razoável não basta para possuir uma saída.
\footnote{A derivação da saída e do \emph{view tag} está em
\path{src/device/device_default.cpp}; a máscara e o XOR de oito bytes, em
\path{src/ringct/rctOps.cpp}; e a forma serializada, em
\path{src/ringct/rctTypes.h}.}

Uma transacção publica uma chave principal de transacção em
\texttt{extra}. Quando há sub-endereços, pode publicar ainda uma lista de
chaves adicionais, uma por saída, seguindo exactamente os casos da secção
\ref{sec:multi_out_transactions}. Essas chaves permitem ao destinatário
obter \(D_t\); não demonstram ao consenso que certo endereço foi o
destinatário pretendido.


\subsection{Uma prova Bulletproofs+ para todas as saídas}

Os compromissos \(C^b_1,\ldots,C^b_p\) entram numa única prova
Bulletproofs+ que mostra
\begin{align*}
 b_t\in[0,2^{64}-1]\qquad\text{para todo }t.
\end{align*}
Na transacção ordinária v16, \(2\leq p\leq16\). A prova é preenchida
internamente até
\begin{align*}
 M=2^{\lceil\log_2p\rceil},
\end{align*}
e o verificador exige precisamente o menor \(M\) que cobre as saídas; não se
pode escolher uma prova maior só para mudar o aspecto dos bytes.

O objecto serializado é
\begin{align*}
 \Pi_{\mathrm{BPP+}}=
 (A,A_1,B,r_1,s_1,d_1,\mathbf L,\mathbf R).
\end{align*}
Os compromissos \(V_t\) não são serializados dentro da prova: o nó
reconstrói-os a partir dos \(C^b_t\) que já estão na base RingCT. Há aqui uma
convenção de cofactor que é fácil perder de vista. \texttt{outPk} guarda o
compromisso normal \(C^b_t\), enquanto a prova usa
\(V_t=8^{-1}C^b_t\). Do mesmo modo, \(A,A_1,B,L_i,R_i\) viajam divididos por
8 e o verificador multiplica-os por 8. Isto é representação no fio, não uma
mudança do montante comprometido. A prova e a verificação completas estão na
secção \ref{sec:bulletproof_plus}. Os compromissos históricos do anel, os
compromissos de saída em \texttt{outPk} e os pseudo-compromissos são
serializados na representação normal; não se lhes aplica esta divisão por 8.
\footnote{Comparem-se \path{src/ringct/rctSigs.cpp},
\path{src/ringct/rctTypes.cpp},
\path{src/cryptonote_basic/cryptonote_format_utils.cpp} e
\path{src/ringct/bulletproofs_plus.cc}.}


\subsection{Um anel: uma entrada real e quinze desvios}

Para cada entrada \(j\), a transacção contém 16 índices de saídas históricas.
Depois de converter os \emph{offsets} relativos em índices absolutos, o nó
obtém o anel público
\begin{align}
 \mathcal R_j=
 \bigl\{(P_{i,j},C^a_{i,j})\bigr\}_{i=0}^{15}.
 \label{eq:transaction-ring}
\end{align}
Um desses pares, no índice secreto \(\pi_j\), é a saída que o remetente
possui; os outros quinze são desvios. Os índices absolutos aparecem por
ordem crescente e só o primeiro é guardado em absoluto. Por exemplo,
\(\{7,11,15,20\}\) torna-se \(\{7,4,4,5\}\). A repetição de um índice dentro
do mesmo anel produz um \emph{offset} relativo zero e é rejeitada.

Na versão 16 todas as entradas RingCT têm exactamente quinze desvios, isto
é, dezasseis membros, e todas têm o mesmo tamanho de anel. Isto é consenso.
A escolha de \emph{quais} saídas ocupam os quinze lugares é comportamento da
carteira. O programa de referência insere a saída real e amostra candidatos
históricos com a sua \texttt{gamma\_picker}: parte de uma distribuição gamma
de forma \(19{,}28\) e escala \(1/1{,}61\), corrige a janela de maturação,
estima um índice pela densidade recente de saídas e escolhe ao acaso uma
saída dentro do bloco encontrado.\footnote{Veja-se
\texttt{src/wallet/wallet2.cpp}, em particular \texttt{gamma\_picker::pick()}
e \texttt{wallet2::get\_outs()}. Esta distribuição é uma heurística de
privacidade da carteira congelada, não uma regra de validade. Também não se
deve chamar aos desvios ``saídas não gastas'': a cadeia não marca uma saída
RingCT como gasta, apenas regista imagens de chave.}

Quem verifica conhece todos os pares de \(\mathcal R_j\), mas não recebe
esses pontos repetidos dentro da assinatura. Recebe os \emph{offsets} e vai
buscá-los à cadeia. Antes de verificar a CLSAG confirma que as referências
existem, têm tipos admissíveis, já maturaram e estão desbloqueadas.


\subsection{Assinatura}
\label{full-signature}

O pseudo-compromisso da entrada \(j\) é subtraído a cada compromisso do
anel:
\begin{align*}
 Q_{i,j}=C^a_{i,j}-\widetilde C^a_j.
\end{align*}
Para os desvios \(i\ne\pi_j\), \(Q_{i,j}\) é apenas um ponto para o qual o
signatário não conhece necessariamente logaritmo em base \(G\). Na coluna
real, os montantes cancelam:
\begin{align}
 Q_{\pi_j,j}
 &=(x_jG+a_jH)-(x'_jG+a_jH)\notag\\
 &=(x_j-x'_j)G=z_jG.
 \label{eq:transaction-zero-commitment}
\end{align}

Assim, para assinar a entrada \(j\), o remetente conhece dois testemunhos na
mesma coluna secreta:
\begin{align*}
 p_j&=\log_G(P_{\pi_j,j}),\\
 z_j&=\log_G(Q_{\pi_j,j})=x_j-x'_j\pmod l.
\end{align*}
A CLSAG comprime estas duas relações numa só assinatura, sem revelar
\(\pi_j\). As imagens ligáveis são
\begin{align*}
 I_j&=p_j\mathcal H_p(P_{\pi_j,j}),\\
 D_j&=z_j\mathcal H_p(P_{\pi_j,j}).
\end{align*}
\(I_j\) é guardada na entrada, antes da parte RingCT. A assinatura CLSAG
serializa, para um anel v16, dezasseis respostas
\(s_{0,j},\ldots,s_{15,j}\), o desafio inicial \(c_{1,j}\) e
\begin{align*}
 \bar D_j=8^{-1}D_j.
\end{align*}
Não serializa \(I_j\) uma segunda vez. Os pesos de agregação, o ciclo de
desafios e as equações completas estão na secção \ref{sec:CLSAG}; repeti-los
aqui só vestiria a mesma assinatura com outro casaco.

Em suma, a \emph{declaração pública} de uma CLSAG desta transacção contém
\(\mathfrak m\), os dezasseis pares
\((P_{i,j},C^a_{i,j})\), \(\widetilde C^a_j\), \(I_j\) e \(\bar D_j\).
Os \emph{testemunhos privados} são o índice \(\pi_j\), a chave de utilização
única \(p_j\) e a diferença de máscaras \(z_j=x_j-x'_j\). Os \emph{hashes} de
agregação incluem ambas as filas, as imagens e o pseudo-compromisso; o ciclo
de desafios usa os dois testemunhos na mesma coluna. É esta coincidência de
índice, e não uma vaga prova de que ``alguém conhece alguma coisa'', que
prende a chave de gasto à diferença correcta de compromissos.

Para \(m\) entradas, a assinatura total é
\begin{align*}
 \tau=\{\sigma_1(\mathfrak m),\ldots,\sigma_m(\mathfrak m)\}.
\end{align*}
Cada entrada tem o seu próprio anel e o seu próprio índice real. Todas as
CLSAG assinam a mesma mensagem \(\mathfrak m\), mas cada uma prende também o
seu pseudo-compromisso à sua assinatura. A transacção só passa se todas
passarem.


\subsection{A mensagem que as CLSAG assinam}

``Assina-se toda a transacção'' é uma boa intuição, mas não é uma descrição
de bytes suficientemente exacta. Na revisão congelada, a mensagem anterior
às CLSAG é
\begin{align}
 \mathfrak m=\mathcal H(h_0\,\|\,h_1\,\|\,h_2),
 \label{eq:transaction-pre-clsag-hash}
\end{align}
onde:

\begin{description}
    \item[\(h_0\)] é o \emph{hash} do prefixo serializado: versão,
    \texttt{unlock\_time}, entradas (montantes visíveis zero, \emph{offsets}
    e imagens de chave), saídas (montantes visíveis zero, tipo de alvo,
    endereços ocultos e etiquetas de visualização) e os bytes exactos de
    \texttt{extra};

    \item[\(h_1\)] é o \emph{hash} da base RingCT serializada: tipo `6',
    taxa, os oito bytes cifrados de cada montante e os compromissos normais
    de saída em \texttt{outPk};

    \item[\(h_2\)] é o \emph{hash} da prova Bulletproofs+ pela ordem
    \(A,A_1,B,r_1,s_1,d_1,L_0,\ldots,L_q,R_0,\ldots,R_q\).
\end{description}

Os corpos CLSAG ficam necessariamente de fora, pois ainda estão a ser
calculados. Há uma subtileza menos óbvia: os pseudo-compromissos também não
entram em \(h_0,h_1,h_2\). Em vez disso, a transcrição de cada CLSAG inclui
explicitamente o seu próprio \(\widetilde C^a_j\), juntamente com
\(\mathfrak m\), os \(P_{i,j}\), os \(C^a_{i,j}\), \(I_j\) e \(D_j\). Logo,
mudar um pseudo-compromisso invalida a assinatura correspondente; não é, porém,
verdade literal que exista um único \emph{hash} de todos os bytes excepto as
assinaturas.\footnote{A construção está em
\path{src/ringct/rctSigs.cpp}, funções
\path{get_pre_mlsag_hash()}, \path{genRctSimple()} e
\path{CLSAG_Gen()}; o \emph{hash} final dos três termos está em
\path{src/device/device_default.cpp}.}

Esta disposição prende os destinatários, os anéis, as imagens de chave, a
taxa, \texttt{extra}, os montantes em código, os compromissos de saída, a
prova de domínio e cada pseudo-compromisso à autorização do dono. Uma
alteração deixa de fechar pelo menos um ciclo CLSAG.

Cada camada responde apenas à sua pergunta. Bulletproofs+ não prova
propriedade, destinatário, ausência de gasto duplo nem validade completa da
transacção. CLSAG não prova que os novos montantes estão no domínio. A equação
de equilíbrio não autoriza uma entrada. A transacção só é válida quando estas
respostas, a serialização e as regras exteriores de consenso passam em
conjunto.


\subsection{Verificação}
\label{full-verify}

A implementação divide a verificação RingCT em duas partes com nomes um
pouco enganadores:

\begin{enumerate}
    \item a verificação \emph{semântica} confirma a forma dos vectores, a
    correspondência entre saídas e montantes em código, a existência de uma
    única prova Bulletproofs+ com a capacidade exacta, a equação
    \eqref{eq:transaction-commitment-balance} e a prova de domínio;

    \item a verificação \emph{não semântica} reconstrói os anéis a partir da
    cadeia e verifica uma CLSAG por entrada contra o seu
    pseudo-compromisso.
\end{enumerate}

Em redor destas duas partes, o núcleo verifica ainda a versão e o tipo, os
alvos das saídas, o número e a igualdade dos tamanhos dos anéis, os
\emph{offsets}, a existência e maturidade das referências, as imagens de
chave e o limite de peso. ``Semântica'' não quer dizer aqui ``tudo o que a
transacção significa''; é o nome histórico da metade que pode ser verificada
sem procurar os membros dos anéis.

Os escalares de resposta e desafio CLSAG têm de estar em forma canónica. Os
pontos têm de descodificar, \(I_j\) não pode ser a identidade e tem de
satisfazer \(lI_j=0\); depois de recuperar \(D_j=8\bar D_j\), a verificação
rejeita também a identidade. Bulletproofs+ verifica os escalares canónicos,
a forma dos vectores e as suas próprias equações depois das multiplicações
por 8. É por isso demasiado forte dizer simplesmente que ``todos os pontos
da transacção são testados um a um no subgrupo primo'': a implementação usa
testes e limpezas de cofactor diferentes em fronteiras diferentes.

Os testes locais tornam estas fronteiras visíveis. Os testes de núcleo para
Bulletproofs+ rejeitam provas em falta, provas duplicadas e uma prova sobre
o compromisso errado; o teste de maleabilidade CLSAG altera \(\bar D\) por
um ponto de torção e espera rejeição; os testes de serialização confirmam
que \(V_t\) e \(I_j\) são reconstruídos, não repetidos no corpo das provas.\footnote{
Veja-se \path{tests/core_tests/bulletproof_plus.cpp},
\path{tests/core_tests/rct2.cpp},
\path{tests/unit_tests/bulletproofs_plus.cpp} e
\path{tests/unit_tests/serialization.cpp}.}


\subsection{Evitar o gasto duplo}

A imagem de chave \(I_j=p_j\mathcal H_p(P_{\pi_j,j})\) é determinística para
a chave privada de utilização única, mas não revela qual dos dezasseis
\(P_{i,j}\) lhe corresponde. O núcleo rejeita imagens repetidas dentro da
mesma transacção, entre transacções do mesmo bloco e contra o conjunto de
imagens já gastas na cadeia. Sob a segurança da CLSAG e da função de
\emph{hash} para ponto, uma segunda assinatura com a mesma chave produz a
mesma \(I_j\): o marcador denuncia a repetição sem desmascarar o membro real.

A igualdade de uma imagem de chave não diz quanto foi gasto, para onde foi,
nem qual membro do anel era verdadeiro. Diz apenas que duas tentativas não
podem ambas entrar na história canónica. Antes disso, a \emph{mempool}
também evita aceitar conflitos que conhece; a regra decisiva é a validação
do bloco.


\subsection{Maturação e tempo de desbloqueio}

Todas as saídas referidas por um anel v16, verdadeira e desvios, têm de ter
pelo menos dez blocos. Uma saída criada no bloco de altura \(h\) pode, por
esta regra, aparecer pela primeira vez num anel de uma transacção incluída à
altura \(h+10\). Não é apenas o comportamento por omissão da carteira: desde
v12 o núcleo impõe-o ao conjunto inteiro de referências.

O prefixo contém ainda um único \texttt{unlock\_time} para todas as saídas
da transacção. Valores inferiores a \(500\,000\,000\) são interpretados como
altura; valores a partir daí, como tempo Unix. Na versão 16, o teste temporal
usa o tempo ajustado derivado dos blocos anteriores e uma tolerância de 120
segundos. A carteira de referência constrói transacções ordinárias com
\texttt{unlock\_time}=0, e os nós não propagam normalmente uma transacção
ordinária com outro valor; isto é política de \emph{mempool}. O consenso
conserva a funcionalidade e verifica também o \texttt{unlock\_time} original
de cada membro do anel.

Uma transacção de mineiro criada à altura \(h\) tem obrigatoriamente
\(\texttt{unlock\_time}=h+60\). A sua saída só pode portanto ser referida a
partir dessa altura; o bloqueio de 60 domina a maturação ordinária de 10.\footnote{
Veja-se \path{is_tx_spendtime_unlocked()} e a verificação das referências
em \path{src/cryptonote_core/blockchain.cpp}, a política em
\path{src/cryptonote_core/tx_pool.cpp} e a construção da transacção de
mineiro em \path{src/cryptonote_core/cryptonote_tx_utils.cpp}.}


\subsection{o segredo público \texorpdfstring{$\gamma$}{gamma}}
\label{gamma}

Chamemos ``segredo público'' ao segredo que toda a gente sabe que existe e
ninguém deve saber qual é. O ponto \(H\) é público e, por viver no subgrupo
gerado por \(G\), existe matematicamente um escalar \(\gamma\) tal que
\begin{align*}
 H=\gamma G.
\end{align*}
O que se exige é que ninguém conheça esse escalar. A construção de \(H\) por
\emph{hash} para ponto destina-se precisamente a não deixar uma pessoa com
esta porta das traseiras. \(\gamma\) não é, portanto, um segredo publicamente
conhecido: é o logaritmo discreto desconhecido por detrás de uma relação
pública. Daqui em diante, toda a aritmética escalar desta conversa é módulo
\(l\).

Suponha-se, contudo, que o Óscar conhece \(\gamma\). Compra ou recebe
\(0{,}1\) xmr numa saída já gastável que possui. Conhece a chave de gasto, o
montante
\(b\), a máscara \(y\) e o compromisso histórico
\begin{align*}
 C^a=yG+bH.
\end{align*}
Não precisa de publicar um novo ponto \(C^a_{\mathrm{mau}}\). Escolhe qualquer
montante de 64 bits e quer uma \emph{segunda abertura do mesmo ponto}; para
continuar a história, toma \(b_{\mathrm{mau}}=100\) xmr. Define
\begin{align}
 y_{\mathrm{mau}}
   =y+(b-b_{\mathrm{mau}})\gamma\pmod l.
 \label{eq:gamma-bad-mask}
\end{align}
Então
\begin{align*}
 y_{\mathrm{mau}}G+b_{\mathrm{mau}}H
 &=\bigl(y+(b-b_{\mathrm{mau}})\gamma\bigr)G
      +b_{\mathrm{mau}}\gamma G\\
 &=yG+b\gamma G\\
 &=C^a.
\end{align*}
O ponto na cadeia não mexeu um milímetro; foi a história que o Óscar conta
sobre ele que engordou.

Agora podemos seguir a burla até ao fim, em vez de parar à porta. Ao gastar
a saída, o Óscar trata \(C^a\) como compromisso a
\(b_{\mathrm{mau}}\). Escolhe uma pseudo-máscara \(x'\) e publica
\begin{align*}
 \widetilde C^a=x'G+b_{\mathrm{mau}}H.
\end{align*}
Na coluna real do anel,
\begin{align*}
 C^a-\widetilde C^a
   =(y_{\mathrm{mau}}-x')G,
\end{align*}
portanto conhece o segundo testemunho CLSAG
\(z=y_{\mathrm{mau}}-x'\). Continua a precisar de \(p\), a chave de gasto da
saída: conhecer \(\gamma\) não rouba moedas alheias por si só. Mas permite
reabrir as próprias moedas com montantes inventados.

O Óscar cria, por exemplo, duas saídas cuja soma, mais a taxa, é
\(b_{\mathrm{mau}}\). Escolhe as máscaras de saída, fecha
\eqref{eq:transaction-commitment-balance}, produz uma Bulletproofs+ válida
para esses dois novos montantes de 64 bits e assina cada entrada. Tudo passa:
a CLSAG conhece \(p\) e \(z\), o equilíbrio usa a abertura falsa e a prova de
domínio só pergunta se os \emph{novos} montantes de saída estão no domínio.
A prova de domínio que acompanhou \(C^a\) quando este nasceu demonstrava que
existia uma abertura antiga em 64 bits; não demonstrava que essa abertura
era única, e o gasto não volta a provar o domínio das entradas.

Sem \(\gamma\), o mesmo truque pediria
\begin{align*}
 y_{\mathrm{mau}}
  =\log_G\!\left(C^a-b_{\mathrm{mau}}H\right),
\end{align*}
e agora, ``ah e tal'', queremos calcular um logaritmo discreto. É aqui que o
PLD fecha a porta. Mais ainda, se alguém exibisse duas aberturas distintas
\((y,b)\) e \((y_{\mathrm{mau}},b_{\mathrm{mau}})\) do mesmo compromisso,
extrairíamos imediatamente
\begin{align}
 \gamma=
 (y_{\mathrm{mau}}-y)
 (b-b_{\mathrm{mau}})^{-1}\pmod l.
 \label{eq:gamma-extraction}
\end{align}
O inverso existe: dois montantes distintos de 64 bits têm diferença não
nula módulo o primo \(l\). Encontrar uma segunda abertura não trivial quebra,
pois, a vinculação computacional e revela exactamente
\(\gamma=\log_GH\), a relação de logaritmo discreto entre os geradores.

É importante ler a frase na direcção certa. O compromisso \(C^a\), sozinho,
não entrega \(\gamma\). Para o extrair pela equação
\eqref{eq:gamma-extraction}, seria preciso conhecer as duas aberturas
completas: \((y,b)\) e
\((y_{\mathrm{mau}},b_{\mathrm{mau}})\). Mas suponha-se que existia uma
máquina capaz de receber a primeira e encontrar facilmente uma segunda.
Bastava aplicar essa equação à resposta da máquina e teríamos um algoritmo
para calcular \(\log_GH\). Portanto, encontrar uma segunda abertura não é uma
porta lateral mais barata para evitar o PLD; se o fosse, essa mesma porta
resolveria o PLD.

Os 64 bits dos montantes também não encolhem o problema para 64 bits. Depois
de escolher um candidato \(b_{\mathrm{mau}}\), o Óscar consegue calcular o
ponto
\begin{align*}
 P=C^a-b_{\mathrm{mau}}H,
\end{align*}
mas continua a precisar do escalar \(y_{\mathrm{mau}}\) tal que
\(P=y_{\mathrm{mau}}G\). Como \(G\) gera o subgrupo, qualquer montante candidato
produz um ponto que tem algum logaritmo; o compromisso não acende uma luz para
dizer se esse montante é o histórico. O obstáculo é calcular o logaritmo
correspondente, um PLD no grupo inteiro, cujo tamanho é \(l\), não uma busca no
intervalo dos montantes.

Há, claro, quem se esfole a tentar arrombar esta porta. Os melhores ataques
\emph{genéricos} clássicos conhecidos, como o método rho de Pollard, precisam
de uma ordem de grandeza de
\begin{align*}
 \sqrt l
\end{align*}
operações de grupo: encontram uma colisão depois de explorar aproximadamente
a raiz quadrada do espaço, pelo mesmo efeito do paradoxo dos aniversários.
Aqui \(l\) está um pouco acima de \(2^{252}\), logo \(\sqrt l\) está perto de
\(2^{126}\), cerca de \(8{,}5\times10^{37}\) operações. Mesmo uma máquina
fantasiosa que executasse \(10^{18}\) operações de grupo por segundo levaria
cerca de \(2{,}7\times10^{12}\) anos. Uma operação de grupo real é, de resto,
muito mais trabalhosa do que uma instrução elementar de processador.

``Genérico'' quer dizer que o ataque usa apenas as operações do grupo e não
uma fraqueza especial da curva, da geração de \(H\) ou da implementação. A
estimativa \(2^{126}\) não é uma prova de que nunca surgirá matemática melhor;
é o nível de segurança clássico que resulta dos melhores métodos conhecidos.
Um atalho genuíno não faria apenas o Óscar rico: abalaria todas as construções
que dependem do mesmo PLD. É por isso que as pessoas tentam --- e é também por
isso que não lhes basta tentar com um computador um bocadinho maior.

O segredo de \(\gamma\) é, pois, a hipótese de \emph{vinculação} dos
compromissos e da oferta monetária. Não é a hipótese de ocultação: com uma
máscara uniforme, \(yG+bH\) esconde perfeitamente \(b\) mesmo de quem
conhecesse \(\gamma\). E os muitos logaritmos que aparecem ao variar
\(b_{\mathrm{mau}}\) não são segredos independentes; são transformações
afins da mesma porta das traseiras \(\gamma\). Se ela se abre, pode-se
``gamar''; se fica fechada, cada transformação continua a parecer um PLD
novo.

Finalmente, três gammas que partilham a letra não partilham a função. Este
\(\gamma=\log_GH\) é a relação desconhecida entre geradores. A distribuição
gamma da \texttt{gamma\_picker} escolhe idades de desvios. E os símbolos
\(\gamma_j\) usados por vezes na literatura de Bulletproofs para máscaras de
compromissos são apenas escalares escolhidos pelo provador. Confundi-los é
dar a mesma alcunha ao cofre, ao carteiro e à cortina.


\subsubsection*{Provas de domínio (saídas)}

Uma prova Bulletproofs+ agregada, com \(M\) a menor potência de dois tal que
\(M\geq p\), requer
\begin{align*}
 32\bigl(6+2\log_2(64M)\bigr)\ \text{bytes}
\end{align*}
para os seus elementos de 32 bytes, além dos prefixos de comprimento dos
vectores. Para \(p=2\), são 640 bytes; para \(p=16\), 832 bytes.

\newpage
\section{Resumo conceptual}
\label{sec:transaction_summary}

Para resumir, apresenta-se aqui o conteúdo principal de uma transacção v16,
organizado para clareza conceptual. O apêndice
\ref{appendix:RCTTypeBulletproof2} mostra bytes reais de uma transacção mais
antiga; as diferenças estão assinaladas no próprio apêndice.

\begin{itemize}
    \item \underline{Tipo}: `6' é \texttt{RCTTypeBulletproofPlus}; `0' é
    \texttt{RCTTypeNull} para a transacção de mineiro.

    \item \underline{Entradas}: para cada entrada \(j\):
    \begin{itemize}
        \item montante visível zero;
        \item dezasseis \emph{offsets} relativos que localizam o anel;
        \item imagem de chave \(I_j\);
        \item pseudo-compromisso \(\widetilde C^a_j\), na parte podável;
        \item CLSAG com dezasseis \(s_{i,j}\), \(c_{1,j}\) e
        \(\bar D_j\), também na parte podável.
    \end{itemize}

    \item \underline{Saídas}: para cada saída \(t\):
    \begin{itemize}
        \item montante visível zero;
        \item alvo etiquetado com o endereço oculto \(P^b_t\) e um
        \emph{view tag} de um byte;
        \item compromisso normal \(C^b_t=y_tG+b_tH\) em \texttt{outPk};
        \item oito bytes \(b_t\) cifrados por XOR em \texttt{ecdhInfo}.
    \end{itemize}

    \item \underline{Base RingCT}: tipo, taxa pública, \texttt{ecdhInfo} e
    \texttt{outPk}. Esta parte não é podável.

    \item \underline{Parte podável}: uma prova Bulletproofs+,
    seguida das CLSAG e dos pseudo-compromissos.

    \item \underline{\texttt{extra}}: bytes opacos no prefixo, onde as
    carteiras colocam normalmente a chave pública principal de transacção,
    eventuais chaves adicionais e, quando aplicável, um \emph{nonce}. O
    consenso assina os bytes exactos, mas não prova que estes obedecem às
    relações secretas pretendidas pela carteira.
\end{itemize}

\newpage
Finalmente, uma transacção exemplo com uma entrada e duas saídas soa assim:

``A transacção publica as chaves necessárias para os destinatários
reconhecerem as saídas. Gasto uma das dezasseis saídas do meu anel. A CLSAG
prova que conheço, na mesma posição escondida, a chave privada do endereço
oculto e a diferença de máscaras entre o compromisso histórico e o
pseudo-compromisso. A imagem de chave ainda não apareceu, portanto esta
saída não foi aceite antes noutro gasto. Crio duas saídas com endereços
ocultos, etiquetas de visualização, compromissos e montantes em código. Uma
Bulletproofs+ prova que ambos os montantes estão no domínio de 64 bits. A
taxa \(f\) é pública, e
\(\widetilde C^a=C^b_1+C^b_2+fH\). A mensagem CLSAG prende o prefixo, a base
RingCT e a prova, e a transcrição da própria CLSAG prende ainda o
pseudo-compromisso. Não vos digo qual entrada era minha nem quanto transferi;
dou-vos exactamente as provas necessárias para não ter de mo dizer.''


\subsection{Requisitos de espaço}
\label{subsec:space-and-ver-rcttypefull}

Um anel v16 não repete no \emph{blob} os 16 endereços ocultos e os 16
compromissos: os \emph{offsets} apontam para esses dados na cadeia. Por
entrada, os elementos fixos directamente ligados ao gasto são:
\begin{align*}
 \underbrace{16\cdot32+32+32}_{\text{CLSAG}}
 +\underbrace{32}_{I_j}
 +\underbrace{32}_{\widetilde C^a_j}
 =640\ \text{bytes},
\end{align*}
antes dos dezasseis \emph{varints} dos \emph{offsets} e dos pequenos
prefixos de serialização. A CLSAG propriamente dita ocupa 576 bytes: 512 nas
respostas, 32 em \(c_1\) e 32 em \(\bar D\).

Para as saídas, cada \(t\) acrescenta pelo menos os 32 bytes do endereço
oculto, um byte de \emph{view tag}, 32 bytes do compromisso e oito bytes do
montante em código, mais as marcas e comprimentos da serialização. As chaves
de transacção vivem em \texttt{extra}. A prova Bulletproofs+ custa
\begin{align*}
 32\bigl(6+2\log_2(64M)\bigr),
 \qquad M=2^{\lceil\log_2p\rceil},
\end{align*}
mais dois comprimentos de vector.

\subsubsection*{Peso, não apenas bytes}

Para uma transacção actual, o peso é o tamanho serializado mais uma
penalização de ``recuperação'' da economia obtida pela agregação de
Bulletproofs+. Se \(M\leq2\), a penalização é zero e peso e bytes coincidem.
Para \(M>2\), a revisão congelada calcula
\begin{align*}
 w={}&|\mathrm{blob}|+
 \frac{4}{5}\left[
 320M-32\bigl(6+2\log_2(64M)\bigr)
 \right],
\end{align*}
com divisão inteira no último termo. Na versão 16 uma transacção não pode
exceder
\begin{align*}
 \frac{300\,000}{2}-600=149\,400
\end{align*}
unidades de peso. A reserva de 600 deixa espaço para a transacção de mineiro;
a relação com o peso dinâmico do bloco é explicada na secção
\ref{subsec:dynamic-block-weight}.\footnote{Veja-se
\path{get_transaction_weight()} e
\path{get_transaction_weight_clawback()} em
\path{src/cryptonote_basic/cryptonote_format_utils.cpp}, e o limite em
\path{src/cryptonote_core/tx_verification_utils.cpp}.}

\subsubsection*{O campo \texttt{extra}: conteúdo, limite e fronteira}

\texttt{extra} é um vector de bytes do prefixo. As convenções conhecidas
começam cada campo por uma etiqueta: por exemplo, \texttt{0x01} para a chave
pública principal, \texttt{0x02} para um \emph{nonce} com comprimento e
\texttt{0x04} para chaves públicas adicionais; \texttt{0x00} representa
enchimento. Portanto, não é verdade que as etiquetas tenham ``todos os bits
a um''.

A carteira de referência ordena campos que consegue analisar e recusa criar
\texttt{extra} com mais de 1060 bytes. Um nó recusa normalmente propagar pela
\emph{mempool} uma transacção que exceda esse valor. O limite de 1060 é
política, não uma regra de consenso do bloco: no consenso, \texttt{extra}
continua limitado indirectamente pelo tamanho e pelo peso de toda a
transacção.

Também aqui ``não é verificado'' precisa de tradução. O vector e o seu
comprimento têm de ser serializáveis, e os bytes exactos entram em \(h_0\),
logo não podem ser alterados depois da assinatura. Mas a validação de um
bloco não exige que esses bytes formem todos campos reconhecidos, que haja
uma só chave de cada espécie, nem que uma chave de transacção corresponda a
um segredo usado pelo remetente. Essas são convenções que permitem às
carteiras encontrar e decifrar saídas. Bytes mal formados podem tornar uma
saída invisível ou inutilizável sem, por essa razão isolada, tornar inválido
o bloco.\footnote{As etiquetas e formatos estão em
\path{src/cryptonote_basic/tx_extra.h}; a análise e ordenação, em
\path{src/cryptonote_basic/cryptonote_format_utils.cpp}; os limites de
construção e propagação, em \path{cryptonote_tx_utils.cpp} e
\path{tx_pool.cpp}.}

É uma fronteira característica de Monero: o consenso protege a integridade
dos bytes e as equações monetárias; a carteira dá significado operacional a
alguns desses bytes. Confundir as duas coisas faz o protocolo parecer ao
mesmo tempo mais rígido e menos seguro do que realmente é.
